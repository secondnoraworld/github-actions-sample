name: "test"
description: "test"
author: "noraworld"
# branding:
#   icon: search
#   color: green

inputs:
  from:
    description: "TBD"
    required: true
  into:
    description: "TBD"
    required: true

runs:
  using: "composite"
  steps:
    - name: Enable Git operation
      uses: actions/checkout@v2
      with:
        fetch-depth: 0

    - name: check
      run: |
        test "$(git rev-list --count ${{ inputs.from }}..${{ inputs.into }})" -eq 0
      shell: sh
